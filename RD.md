# タスク管理アプリ 要件定義書

## 1. 背景・目的

スキルアップを目的とした個人開発のポートフォリオアプリ。以下の課題を解決する。

- タスクが多すぎて、何をすべきか考える時間がもったいない
- タスクの締切(ToDo)と、それを実行する時間(カレンダー)が連携されていない

## 2. 開発条件

| 項目 | 内容 |
|---|---|
| 想定期間 | 約2か月 |
| 開発者スキルレベル | プログラミング初級(JavaScript未経験、HTML/CSSは少し経験あり) |
| 技術スタック | HTML / CSS / JavaScript(素のJS。フレームワーク不使用) |
| 外部連携 | なし(Googleカレンダー等の外部API連携は行わない。アプリ内で完結) |
| データ保存 | localStorage を想定 |

## 3. 機能要件

### 3.1 タスク管理(ToDo)【MVP】

- タスクの追加・編集・削除
- タスクの完了チェック
- タスクが持つ属性:
  - タイトル
  - 締切日
  - 所要時間(段階選択式。例: 15分 / 30分 / 1時間 / 2時間以上)
  - 場所(段階選択式。例: 自宅 / 外出先 / どこでも)
  - 割り当て済みフラグ(カレンダーに配置済みかどうかを内部管理)

### 3.2 タスク提案機能【MVP】

- 目的: 「今何をすべきか迷う」を解消するため、状況に応じておすすめのタスクを提示する
- **表示ロジック**:
  1. 現在時刻が、カレンダー上に割り当て済みのタスクの時間帯(開始〜終了)に含まれているか判定する
  2. **含まれる場合** → そのタスクを「今やるタスク」として表示する(提案ロジックはスキップ)
  3. **含まれない場合** → 「今使える時間」「今いる場所」をユーザーが選択し、条件(所要時間が使える時間内に収まる/場所が一致する)に合致する **未割り当て・未完了** タスクを絞り込んで提案する
- 提案時は締切が近い順などで並び替えて提示
- **カレンダーに割り当て済みのタスクは提案候補から除外する**(2つの機能の競合を防ぐルール)

### 3.3 カレンダー割り当て機能

- 目的: ToDoと実行時間の連携を実現する
- アプリ内完結の簡易カレンダーUIを用意(外部カレンダーとの同期はしない)
- タスクをドラッグ&ドロップでカレンダー上の時間帯に配置できる
- 配置したタスクはカレンダー上に表示される
- 配置すると、該当タスクの「割り当て済みフラグ」が立ち、タスク提案の対象から外れる
- 表示単位: 日表示のみ(週表示は対象外)
- タイムグリッドの粒度: グリッド線・ドロップ時のスナップともに1時間刻み(例: 9:00, 10:00, 11:00…の位置にのみ配置可能)
- 所要時間が「2時間以上」のタスクは、カレンダー上で2時間分のブロックとして表示する(下記6.2参照)

#### 実装フェーズ分け

| フェーズ | 内容 |
|---|---|
| MVP | タスクをそのまま1ブロックとしてD&D配置(分割なし) |
| 発展機能(余裕があれば) | 所要時間分のタスクブロックを、複数の時間帯に分割して配置できる |

## 4. 開発の優先順位(実装順)

1. タスク管理(CRUD)の実装
2. タスク提案機能の実装(ロジック中心、JS練習にも最適)
3. カレンダー機能(D&Dによるタスク割り当て)
4. 余裕があれば: タスク分割機能

※ 初級者・2か月という制約の中で「確実に完成させること」を優先するため、上記の順序と段階分けを設計指針とする。

## 5. 画面構成

### 5.1 画面遷移方式

- 別HTMLファイルには分けず、1つのHTMLファイル内でJavaScriptによる表示切り替え(タブ切り替え)で実現する
- 上部タブで「今すぐ」「タスク一覧」「カレンダー」の3画面を切り替える

### 5.2 画面一覧

| 画面 | 対応する機能要件 | 概要 |
|---|---|---|
| 画面1: 今すぐ(ホーム) | 3.2 タスク提案機能 | 現在時刻が割当済みタスクの時間帯に含まれる場合は「今やるタスク」を表示。含まれない場合は「今使える時間」「今いる場所」を選択し、条件に合う未割当・未完了タスクを提案 |
| 画面2: タスク一覧 | 3.1 タスク管理(ToDo) | タスクのCRUD、完了チェック。追加・編集はモーダルで行う |
| 画面3: カレンダー | 3.3 カレンダー割り当て機能 | 左に未割当タスク一覧、右に1日分のタイムグリッド(1時間刻み)。D&Dでタスクを配置 |

### 5.3 タスク追加・編集フォーム

- モーダル(ポップアップ)方式を採用
- 追加・編集で同一のフォームを共通利用する

## 6. データ構造設計

### 6.1 タスクオブジェクト(localStorage: key = "tasks")

タスク一覧はJSON配列としてlocalStorageに保存する。各タスクは以下の構造を持つ。

```json
[
  {
    "id": "task-001",
    "title": "資料作成",
    "deadline": "2025-08-15",
    "duration": "30min",
    "location": "anywhere",
    "completed": false,
    "assigned": false,
    "scheduledDate": null,
    "scheduledStartHour": null,
    "createdAt": "2025-08-01T10:00:00"
  }
]
```

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | string | 一意なID(`crypto.randomUUID()` などで生成) |
| `title` | string | タスクのタイトル |
| `deadline` | string | 締切日。`YYYY-MM-DD`形式(時刻は持たない) |
| `duration` | string(enum) | 所要時間。`"15min"` / `"30min"` / `"1hour"` / `"2hour+"` の4値 |
| `location` | string(enum) | 場所。`"home"` / `"out"` / `"anywhere"` の3値 |
| `completed` | boolean | 完了チェック |
| `assigned` | boolean | カレンダーに割り当て済みかどうか |
| `scheduledDate` | string \| null | 割り当て先の日付。`YYYY-MM-DD`(未割当なら`null`) |
| `scheduledStartHour` | number \| null | 割り当て先の開始時刻(9, 10, 11…の整数。未割当なら`null`) |
| `createdAt` | string | 作成日時(ISO形式)。並び替えや将来の履歴機能用 |

### 6.2 カレンダー表示ブロックの長さ変換

`duration`(enum文字列)は、カレンダー上での表示ブロックの高さ(占有する時間コマ数)にコード側で変換する。この変換テーブルは`tasks`配列には保存せず、アプリのコード内の定数として持つ(duration自体から一意に決まるため、データの重複を避ける)。

```js
// duration → カレンダー上のブロックの高さ(時間数)への変換テーブル
const DURATION_TO_BLOCK_HOURS = {
  "15min": 1,
  "30min": 1,
  "1hour": 1,
  "2hour+": 2, // MVPでは「2時間以上」は一律2時間ブロックとして扱う
};
```

- `scheduledStartHour`が9で`duration`が`"2hour+"`なら、9:00〜11:00の2マス分を占有するブロックとして描画する
- タスク自体は分割しない(MVPの方針どおり)ので、開始時刻+ブロック時間数だけで配置・描画を計算できる

### 6.3 各画面とデータ構造の対応

- **画面1(今すぐ)**: `assigned === false && completed === false`で絞り込み、`duration`/`location`で条件マッチさせて提案
- **画面2(タスク一覧)**: `tasks`配列を直接CRUD
- **画面3(カレンダー)**: `assigned === true`のタスクを`scheduledDate`でフィルタし、`scheduledStartHour` + `DURATION_TO_BLOCK_HOURS[duration]`で配置・描画
